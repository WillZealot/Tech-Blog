
    <div class="card mb-4">
      <div class="card-header">
        <h5>{{post.title}}: Created: {{post.date_created}}</h5>
      </div>
      <div class="card-body">
        <p class="card-text">{{post.content}}</p>
        <footer class="blockquote-footer">By: {{post.user.name}}</footer>
      </div>
    </div>
    <button id="submitPost" type="submit" class="btn btn-danger">Delete Post</button>
          </br>
    </br>
    <div>
      <div class="mb-4">
  <h3>Update Your Post</h3>
</div>
<form class="editPost" action="/dashboard" id="updatePostForm" method="PUT" data-post-id="{{post.id}}">
  <div class="form-group">
    <label for="title">Revised Title</label>
    <input id="title" name="title" type="text" class="form-control" placeholder="Title...">
  </div>
  <div class="form-group">
    <label for="content">Revised Content</label>
    <input class="form-control" id="content" name="content" placeholder="Post what you want...">
  </div>
  <button type="submit" class="btn btn-primary">Update</button>
</form>
    </div>


<script src="/comment.js"></script>
